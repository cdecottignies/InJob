const advertisements = require('./advertisements.validator')
const companies = require('./companies.validator')
const users = require('./users.validator')
const applicants = require('./applicants.validator')

module.exports = {
    advertisements,
    companies,
    users,
    applicants
}